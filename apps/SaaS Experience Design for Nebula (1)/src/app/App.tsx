// Route: /guest
// Entry point for the Guest Experience.
// GuestPage is the single evolving workspace — no route changes during understanding.

import GuestPage from "./guest/GuestPage";

export default function App() {
  return <GuestPage />;
}
